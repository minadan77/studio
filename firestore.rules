rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any authenticated user to read/write to the shifts collection
    match /shifts/{shiftId} {
      allow read, write: if request.auth != null;
    }
  }
}
